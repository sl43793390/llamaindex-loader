## 使用说明

1. 默认情况下会在当前目录生成 `report.md` 文件和 `images` 目录。
2. 使用方法：
   - 直接运行 `python pdfParse/test.py` 即可。
   - 也可以修改 `test.py` 中的 `PDF` 变量，指向其他 PDF 文件。

3. pdf转图片的功能是为了遇到那种包含图片的pdf文件，需要将图片提取出来，才能进行后续的处理。比如OCR